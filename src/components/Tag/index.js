import styled from 'styled-components';

const Tag = styled.h2`
  background-color: var(--frontEnd);
  display: inline-block;
  font-size: 5rem;
  font-weight: 400;
  padding: 2rem 1.8rem;
`;

export default Tag;
